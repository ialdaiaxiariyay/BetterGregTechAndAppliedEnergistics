dependencies {
    compileOnly(libs.jetbrains.annotations)

    //gtceu
    modImplementation(forge.gtceu)
    modImplementation(forge.registrate)
    modImplementation(forge.configuration)
    modImplementation(forge.mui)

    //Mixin
    annotationProcessor(variantOf(libs.mixin) { classifier("processor") })
    compileOnly(annotationProcessor(forge.mixinExtras.common.get()))
    modImplementation(api(forge.mixinExtras.forge.get()))

    //jei
    modImplementation(forge.bundles.jei)
    modImplementation(forge.emi)
    modRuntimeOnly(forge.emi.accelerator)

    modImplementation(forge.jade)

    // AE2
    modImplementation(forge.ae2)
    modImplementation(forge.guideme)

    modImplementation(forge.bundles.kjs)

    modImplementation(forge.embeddium)

    modImplementation(forge.curios)

    modRuntimeOnly(forge.jech)

    // ftb
    modRuntimeOnly(forge.ftblibrary)
    modRuntimeOnly(forge.ftbteams)
    modRuntimeOnly(forge.ftbultimine)
    modRuntimeOnly(forge.ftbchunks)
}